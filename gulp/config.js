var src = './src',
    dest = './dist';

module.exports = {
    css: {
        src: src + '/css/**/*.css',
        dest: dest + '/css'
    },

    js: {
        src: src + '/js/**/*.js',
        dest: dest + '/js'
    },

    img: {
        src: src + '/img/**/*',
        dest: dest + '/img'
    },
}